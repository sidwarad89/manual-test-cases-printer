## Test Case 1
Below is a **complete, ready‑to‑run Selenium + Python framework** that follows the Page Object Model (POM) and contains one pytest test function per test case described in the document.  
All files are presented with their relative paths, ready to be placed in a Git repository and pushed to GitHub.

---  
## Repository structure
```
selenium-poc-framework/
│
├─ .github/
│   └─ workflows/
│        └─ ci.yml
│
├─ pages/
│   ├─ __init__.py
│   ├─ base_page.py
│   ├─ main_page.py
│   ├─ printer_list_page.py
│   └─ print_page.py
│
├─ tests/
│   ├─ __init__.py
│   ├─ test_tc_001_discoverable_printers.py
│   ├─ test_tc_002_successful_connection.py
│   ├─ test_tc_003_no_printer_paired.py
│   ├─ test_tc_004_printer_out_of_range.py
│   ├─ test_tc_005_receipt_print_time.py
│   ├─ test_tc_006_paper_out_error.py
│   ├─ test_tc_007_bt_drop_during_print.py
│   ├─ test_tc_008_reprint_same_day.py
│   ├─ test_tc_009_reprint_after_24h.py
│   └─ test_tc_010_concurrent_connection.py
│
├─ conftest.py
├─ config.py
├─ requirements.txt
└─ README.md
```

---  
## 1. `requirements.txt`
```text
selenium>=4.15
pytest>=8.2
pytest-html>=4.1
webdriver-manager>=4.0
```

---  
## 2. `config.py`
```python
# Base configuration for the test suite
BASE_URL = "https://example-printer-app.com"   # <-- replace with the real URL
USERNAME = "test_user"                       # <-- replace with real credentials if needed
PASSWORD = "test_password"
```

---  
## 3. `conftest.py`
```python
import pytest
from selenium import webdriver
from selenium.webdriver.chrome.service import Service as ChromeService
from webdriver_manager.chrome import ChromeDriverManager

@pytest.fixture(scope="session")
def driver():
    """Create a Chrome WebDriver instance for the whole test session."""
    options = webdriver.ChromeOptions()
    options.add_argument("--headless")          # Run headless in CI
    options.add_argument("--disable-gpu")
    options.add_argument("--no-sandbox")
    options.add_argument("--window-size=1920,1080")

    driver = webdriver.Chrome(service=ChromeService(ChromeDriverManager().install()),
                              options=options)
    driver.implicitly_wait(10)
    yield driver
    driver.quit()
```

---  
## 4. `pages/base_page.py`
```python
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.support import expected_conditions as EC

class BasePage:
    """Common functionality for all pages."""

    def __init__(self, driver):
        self.driver = driver
        self.wait = WebDriverWait(driver, 20)

    def open(self, url):
        self.driver.get(url)

    def find(self, locator):
        return self.wait.until(EC.visibility_of_element_located(locator))

    def click(self, locator):
        elem = self.find(locator)
        elem.click()

    def type(self, locator, text):
        elem = self.find(locator)
        elem.clear()
        elem.send_keys(text)

    def get_text(self, locator):
        elem = self.find(locator)
        return elem.text
```

---  
## 5. `pages/main_page.py`
```python
from selenium.webdriver.common.by import By
from .base_page import BasePage

class MainPage(BasePage):
    """Home screen of the printer application."""

    # Locators (replace with real ones)
    CONNECT_PRINTER_BTN = (By.ID, "connect_printer_btn")
    PRINT_RECEIPT_BTN   = (By.ID, "print_receipt_btn")
    ORDER_HISTORY_LINK = (By.ID, "order_history_link")
    SUPERVISOR_PIN_INPUT = (By.ID, "supervisor_pin")
    SUPERVISOR_OK_BTN    = (By.ID, "supervisor_ok")

    def go_to_printer_list(self):
        self.click(self.CONNECT_PRINTER_BTN)

    def start_print(self):
        self.click(self.PRINT_RECEIPT_BTN)

    def open_order_history(self):
        self.click(self.ORDER_HISTORY_LINK)

    def enter_supervisor_pin(self, pin):
        self.type(self.SUPERVISOR_PIN_INPUT, pin)
        self.click(self.SUPERVISOR_OK_BTN)
```

---  
## 6. `pages/printer_list_page.py`
```python
from selenium.webdriver.common.by import By
from .base_page import BasePage

class PrinterListPage(BasePage):
    """Page that shows discoverable / paired printers."""

    # Locators (replace with real ones)
    PRINTER_ROWS = (By.CSS_SELECTOR, ".printer-row")
    PRINTER_NAME = (By.CSS_SELECTOR, ".printer-name")
    CONNECTED_INDICATOR = (By.ID, "connected_indicator")
    NO_PRINTER_MSG = (By.ID, "no_printer_msg")
    CONNECT_NOW_BTN = (By.ID, "connect_now_btn")
    ERROR_OUT_OF_RANGE = (By.ID, "out_of_range_msg")
    ERROR_PAPER_OUT = (By.ID, "paper_out_msg")
    BT_DROP_MSG = (By.ID, "bt_drop_msg")
    DUPLICATE_WATERMARK = (By.ID, "duplicate_watermark")
    CONCURRENT_CONN_MSG = (By.ID, "concurrent_conn_msg")

    def select_printer_by_name(self, name):
        rows = self.driver.find_elements(*self.PRINTER_ROWS)
        for row in rows:
